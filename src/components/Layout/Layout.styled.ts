import styled from 'styled-components'
import { rem } from '../../utils/Theme'

export const StyledLayout = styled.div`
  width: 100%;
  max-width: ${rem(1200)};
  margin: 0 auto;
  padding-top: ${rem(120)};
`
